class Node:
    def __init__(self, data):
        self.pref=None
        self.data = data
        self.nref = None


class Linkedlist:
    def __init__(self):
        self.head = None

    # Create Linked List from a Python list
    def create_LL(self, arr):
        self.head = None

        for x in arr:
            self.add_end(x)

    # Insert at end
    def add_end(self, data):
        new_node = Node(data)

        if self.head is None:
            self.head = new_node
        else:
            n = self.head
            while n.nref is not None:
                n = n.nref
            n.nref = new_node
            new_node.pref = n

    # Print Linked List
    def print_LL(self):
        if self.head is None:
            print("Linked list is empty")
        else:
            n = self.head
            while n is not None:
                print(n.data, end=" -> ")
                n = n.nref
            print("None")

#optimal approch for reverse "singly linked" list T.C O(n) S.C O(1)
    def reverse(self,head):
        curr=head
        prev=None
        while curr:
            next_node=curr.nref
            curr.nref=prev
            prev=curr
            curr=next_node
        return prev
        



#brute force approach T.C O(n) S.C O(n)
    # def reverse(self,head):
    #     temp=self.head
    #     stack=[]

    #     while temp:
    #         stack.append(temp.data)
    #         temp=temp.nref
    #     temp=self.head
    #     while temp:
    #         temp.data=stack.pop()
    #         temp=temp.nref
    #     return self.head


# ---------------- MAIN ----------------

LL1 = Linkedlist()

arr = list(map(int, input("Enter elements: ").split()))

LL1.create_LL(arr)

print("\nLinked List:")
LL1.head = LL1.reverse(LL1.head)

LL1.print_LL()
