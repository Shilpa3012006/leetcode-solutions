class Solution:
    def findSpecialInteger(self, arr: List[int]) -> int:
        n=len(arr)
        dict={}
        for i in arr:
            dict[i]=dict.get(i,0)+1
            if dict[i]>n//4:
                return i   



             
