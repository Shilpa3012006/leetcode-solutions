class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        res=0
        for num in nums:
            res^=num
        return res


# not much efficient but we should remember the logic to fetch key throgh values in dict.
        freq={}
        for num in nums:
            freq[num]=freq.get(num,0)+1
            result = [key for key, value in freq.items() if value == 1]
        return result[0]
