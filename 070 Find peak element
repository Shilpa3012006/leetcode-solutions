#optimal solution using binary search with time complexity of O(log n) and space complexity of O(1)
nums = list(map(int, input().split()))
n = len(nums)
low=0
high=n-1
while low<high:
    mid=(low+high)//2
    if nums[mid]<nums[mid+1]:
        low=mid+1
    else:
        high=mid
print(low)
