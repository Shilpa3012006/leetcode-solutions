class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        k=0
        for i in range(len(nums)):
            if nums[i]!=val:
                nums[k]=nums[i]
                k+=1
        return k
      

"""
       a=[]
       b=[]
       for i in range(len(nums)):
        if nums[i]!=val:
            a.append(nums[i])
        else: 
            b.append(nums[i])
        
        for i in range(len(a)):
            nums[i]=a[i]
       return len(a)
       """
